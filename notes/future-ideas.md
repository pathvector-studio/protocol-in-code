# Future Ideas

- DNS recursive resolution as chained function calls
- TCP handshake as explicit state transitions
- TCP retransmission as timeout-driven branching
- TLS handshake as negotiated capability exchange
- HTTP request lifecycle as layered state mutation
- QUIC stream logic as multiplexed state machines
